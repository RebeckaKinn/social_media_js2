SOCIAL MEDIA APPLICATION - ASSIGNMENT REQUIREMENTS
==================================================

PROJECT GOAL
------------
Build the front end of a social media application using JavaScript and
Noroff's provided API.

Users must be able to register, log in, browse posts, manage their own posts,
view profiles, and follow or unfollow other users.


NON-NEGOTIABLE TECHNICAL REQUIREMENTS
-------------------------------------
- Language: JavaScript
- Front-end frameworks: Not allowed
  - Do not use React, Vue, Angular, or similar frameworks.
- Modules: Use ES6 modules.
- API: Use Noroff's API, including the Auth, Posts, and Profiles endpoints.
- Documentation:
  - Create a README.md explaining how to install and run the project.
  - Add JSDoc annotations to at least three functions.
- UI and CSS:
  - Include at least a basic, usable interface.
  - Only basic styling is required.
- Formatting:
  - Keep the code consistently formatted.
  - Prettier is recommended.
- Deployment:
  - Deploy the finished application using GitHub Pages, Netlify, or Vercel.
- Code structure:
  - Avoid large files containing many unrelated functions.
  - Create modular and reusable code.


REQUIRED FEATURES
-----------------
[ x ] Register a new user on a registration page.
[ x ] Log in as a registered user on a login page.
[ x ] View all posts on the feed page.
[ x ] View a single post by clicking a post in the feed.
[ x ] Create a new post.
[ x ] Edit posts owned by the logged-in user.
[ x ] Delete posts owned by the logged-in user.
[ x ] View all posts created by another user.
[ x ] Follow another user.
[ x ] Unfollow another user.
[ x ] Search posts using a search bar.
[ x ] View the logged-in user's own profile.


REQUIRED PAGES
--------------
[ x ] Login page
[ x ] Registration page
[ x ] Posts/feed page
[ x ] Individual post page
[ x ] Logged-in user's profile page


SUGGESTED PROJECT PHASES
========================

PHASE 1 - PROJECT SETUP AND PLANNING
------------------------------------
Requirements used in this phase:
- Create and clone the GitHub repository.
- Use GitHub Projects to track tasks and progress.
- Set up JavaScript with ES6 modules.
- Plan a modular folder and file structure.
- Set up formatting, preferably with Prettier.

Tasks:
[x] Confirm the repository is created and cloned.
[x] Create a GitHub Project board.
[x] Convert each required feature into a task.
[x] Set up ES6 module entry points and folders.
[x] Set up Prettier or another consistent formatting workflow.
[x] Decide which deployment platform to use.


PHASE 2 - SHARED API AND AUTHENTICATION
---------------------------------------
Requirements used in this phase:
- Use Noroff's API documentation.
- Use the Noroff Auth endpoints.
- Register a new user.
- Log in a registered user.
- Use ES6 modules and reusable functions.

Tasks:
[ x ] Create a reusable API request function.
[ x ] Create authentication token storage and retrieval helpers.
[ x ] Build the registration page and connect it to the API.
[ x ] Build the login page and connect it to the API.
[ x ] Store the authenticated user's token and profile details.
[ x ] Handle and display authentication errors.
[ x ] Redirect users appropriately after authentication.


PHASE 3 - POSTS AND FEED
------------------------
Requirements used in this phase:
- View all posts.
- View an individual post.
- Search posts.
- Use the Noroff Posts endpoints.
- Include a basic usable UI.

Tasks:
[ x ] Build the posts/feed page.
[ x ] Fetch and display all posts.
[ x ] Make each post link to its individual post page.
[ x ] Build the individual post page.
[ x ] Fetch and display a single post.
[ x ] Add a search bar that filters or searches posts.
[ x ] Add loading, empty, and error states.


PHASE 4 - POST MANAGEMENT
-------------------------
Requirements used in this phase:
- Create a post.
- Edit the logged-in user's own posts.
- Delete the logged-in user's own posts.
- Keep code modular and reusable.

Tasks:
[ x ] Build a form for creating posts.
[ x ] Connect post creation to the API.
[ x ] Show edit controls only on posts owned by the logged-in user.
[ x ] Build and connect the edit-post form.
[ x ] Show delete controls only on posts owned by the logged-in user.
[ x ]  Add delete confirmation and connect deletion to the API.
[ x ] Update the interface after create, edit, and delete actions.


PHASE 5 - PROFILES AND SOCIAL FEATURES
--------------------------------------
Requirements used in this phase:
- View the logged-in user's own profile.
- View posts created by another user.
- Follow and unfollow other users.
- Use the Noroff Profiles endpoints.

Tasks:
[ x ] Build the logged-in user's profile page.
[ x ] Display profile information and the user's posts.
[ x ] Build a way to view another user's profile and posts.
[ x ] Add a follow button for other users.
[ x ] Add an unfollow button for followed users.
[ x ] Update follow status in the interface after each action.


PHASE 6 - DOCUMENTATION, TESTING, AND DEPLOYMENT
------------------------------------------------
Requirements used in this phase:
- Add JSDoc annotations to at least three functions.
- Create a README.md explaining how to run the project.
- Test the application and track bugs in GitHub Issues.
- Keep code formatted.
- Deploy to GitHub Pages, Netlify, or Vercel.
- Commit regularly using meaningful commit messages.

Tasks:
[ x ] Add JSDoc annotations to at least three useful functions.
[ x ] Write installation and local-development instructions in README.md.
[ x ] Document the deployed application URL in README.md.
[ ] Format all project files.
[ x ] Test every required user story.
[ x ] Deploy the application.
[ x ] Verify the deployed application works.
[ ] Commit and push the finished work.


FINAL COMPLETION CHECKLIST
==========================

Technical requirements:
[ x ] The application uses JavaScript.
[ x ] No front-end framework is used.
[ x ] ES6 modules are used throughout the application.
[ x ] Noroff Auth, Posts, and Profiles API endpoints are used.
[ x ] At least three functions include JSDoc annotations.
[ x ] README.md explains how to install and run the project.
[ x ] The interface has basic usable styling.
[ x ] The code is consistently formatted.
[ x ] The code is split into modular, reusable files.
[ x ] The application is deployed to GitHub Pages, Netlify, or Vercel.

Required pages:
[ x ] Login page works.
[ x ] Registration page works.
[ x ] Posts/feed page works.
[ x ] Individual post page works.
[ x ] Logged-in user's profile page works.

Required user stories:
[ x ] A new user can register.
[ x ] A registered user can log in.
[ x ] A user can view all posts.
[ x ] A user can view one selected post.
[ x ] A logged-in user can create a post.
[ x ] A logged-in user can edit their own posts.
[ x ] A logged-in user can delete their own posts.
[ x ] A user can view posts created by another user.
[ x ] A logged-in user can follow another user.
[ x ] A logged-in user can unfollow another user.
[ x ] A user can search posts.
[ x ] A logged-in user can view their own profile.

Project process:
[ x ] GitHub Projects is used to track work.
[ x ] Meaningful commits are made regularly.
[ x ] The deployed application has been tested.
[ x ] All required features work without blocking errors.
